# FoundationPose自定义数据集测试指南
- 刘亚宁
本文档详细说明了如何使用自定义数据集来测试FoundationPose的功能。FoundationPose支持两种模式的自定义数据：

1. **基于模型的方法（CAD模型可用）**
2. **无模型的方法（仅使用RGB-D数据）**

## 1. 基于模型的方法（推荐）

这种方法需要提供CAD模型，通常能获得更精确的位姿估计结果。

### 1.1 准备数据

#### 1.1.1 RGB和深度图像

- **RGB图像**：标准RGB图像，格式为`.png`，需与深度图像时间戳匹配
- **深度图像**：对应的深度图像，格式为`.png`，与RGB图像具有相同的时间戳和分辨率
- 建议使用800×800分辨率以避免内存问题

#### 1.1.2 遮罩（Mask）

- 只需要为第一帧生成遮罩
- 可以使用提供的[masking.py](file:///wsl.localhost/Ubuntu-20.04/home/lyn/FoundationPose/FoundationPose_manual/masking.py)脚本手动创建遮罩
- 或使用自己的分割模型生成遮罩

#### 1.1.3 CAD模型

- 需要提供目标物体的CAD模型（`.obj`格式）
- 模型单位必须是米（meters）
- 如果使用Blender创建模型，需要应用1000倍缩放以从毫米转换为米

#### 1.1.4 相机内参（cam_K.txt）

- 创建[cam_K.txt](file:///wsl.localhost/Ubuntu-20.04/home/lyn/FoundationPose/FoundationPose_manual/cam_K.txt)文件存储相机内参
- 格式为3x3矩阵，例如：
```
3.195820007324218750e+02 0.000000000000000000e+00 3.202149847676955687e+02
0.000000000000000000e+00 4.171186828613281250e+02 2.443486680871046701e+02
0.000000000000000000e+00 0.000000000000000000e+00 1.000000000000000000e+00
```

### 1.2 数据组织结构
```
your_custom_data/
├── rgb/                 # RGB图像目录
│   ├── 000000.png
│   ├── 000001.png
│   └── ...
├── depth/               # 深度图像目录
│   ├── 000000.png
│   ├── 000001.png
│   └── ...
├── masks/               # 遮罩目录（只需要第一帧）
│   └── 000000.png
├── mesh/                # CAD模型目录
│   ├── model.obj
│   ├── model.mtl
│   └── texture.png
├── cam_K.txt            # 相机内参文件
```

### 1.2.1 生成遮罩（Mask）的详细步骤

生成高质量的遮罩对于FoundationPose的准确性至关重要。以下是生成遮罩的最简便方法：

#### 方法一：使用提供的masking.py脚本（推荐）
1. 修改[masking.py](file:///wsl.localhost/Ubuntu-20.04/home/lyn/FoundationPose/FoundationPose_manual/masking.py)脚本中的图像路径：
   ```python
   # 修改以下行以指向你的RGB图像
   image_path = "your_custom_data/rgb/000000.png"
   mask_save_path = "your_custom_data/masks/000000.png"
   ```

2. 运行脚本：
   ```bash
   python masking.py
   ```

3. 在弹出的窗口中：
   - 使用鼠标左键拖动来绘制遮罩
   - 使用鼠标滚轮调整画笔大小
   - 按's'键保存遮罩
   - 按ESC键退出

#### 方法二：使用第三方工具
1. 使用图像编辑软件（如GIMP、Photoshop）手动绘制遮罩
2. 使用自动分割工具（如Segment Anything Model）生成遮罩
3. 确保遮罩图像为单通道PNG格式，目标物体为白色（255），背景为黑色（0）

### 1.2.2 生成CAD模型（Mesh）的详细步骤

FoundationPose需要一个准确的CAD模型来实现高精度的位姿估计。以下是生成mesh的最简便方法：

#### 方法一：使用Blender创建模型（推荐）

1. 下载并安装[Blender](https://www.blender.org/)
2. 创建新项目并删除默认立方体
3. 导入物体照片或参考图像：
   - 选择"添加" → "图像" → "作为参考"
   - 导入物体的多个角度照片
4. 使用建模工具创建3D模型：
   - 使用"网格" → "立方体"等基本形状开始
   - 使用编辑模式（Tab键）调整顶点、边和面
   - 使用"环切"（Ctrl+R）和"内插"（I）等工具细化模型
5. 设置单位为米：
   - 选择"场景属性" → "单位" → "长度" → "米"
6. 导出模型：
   - 选择"文件" → "导出" → "Wavefront（.obj）"
   - 在导出选项中确保选中"包括" → "写入材质"和"包括" → "选定的物体"
   - 保存为`model.obj`文件

#### 方法二：使用现成的模型

1. 从3D模型网站下载合适的模型（如Thingiverse、CGTrader）
2. 确保模型单位为米，如果不是需要进行缩放
3. 导出为OBJ格式

#### 方法三：使用3D扫描

1. 使用3D扫描设备或手机应用（如Polycam、3D Scanner App）扫描物体
2. 处理扫描数据以获得干净的网格
3. 导出为OBJ格式并确保单位为米

### 1.3 运行测试

1. 复制[demo_data](file:///wsl.localhost/Ubuntu-20.04/home/lyn/FoundationPose/demo_data)目录作为模板：

```bash
cp -r demo_data your_custom_data
```

1. 替换目录中的数据为你的自定义数据

2. 运行测试脚本：

```bash
python run_demo.py --mesh_file your_custom_data/mesh/model.obj --test_scene_dir your_custom_data
```

## 2. 无模型的方法

这种方法不需要CAD模型，仅使用RGB-D数据进行位姿估计。

### 2.1 准备数据

#### 2.1.1 RGB和深度图像

- 同基于模型的方法

#### 2.1.2 遮罩

- 需要为每一帧提供遮罩
- 或使用实时分割方法

#### 2.1.3 相机内参

- 同基于模型的方法

### 2.2 数据组织结构

```
your_custom_data/
├── rgb/
│   ├── 000000.png
│   ├── 000001.png
│   └── ...
├── depth/
│   ├── 000000.png
│   ├── 000001.png
│   └── ...
├── masks/
│   ├── 000000.png
│   ├── 000001.png
│   └── ...
├── cam_K.txt
```

### 2.3 运行测试

对于无模型的方法，需要使用不同的脚本（如果项目提供）或者修改现有脚本以适应无模型的处理流程。

## 3. 数据收集工具

### 3.1 自动数据收集

项目提供了[data_saver.py](file:///wsl.localhost/Ubuntu-20.04/home/lyn/FoundationPose/FoundationPose_manual/data_saver.py)脚本，可以从ROS话题自动收集RGB和深度图像：

```bash
python data_saver.py
```

### 3.2 手动遮罩创建

使用[masking.py](file:///wsl.localhost/Ubuntu-20.04/home/lyn/FoundationPose/FoundationPose_manual/masking.py)脚本可以手动创建遮罩：

```bash
python masking.py
```

## 4. 注意事项

1. **分辨率**：建议使用800×800分辨率，高分辨率可能导致内存不足
2. **时间同步**：RGB和深度图像必须严格时间同步
3. **文件命名**：确保RGB、深度图像和遮罩文件名一致
4. **单位一致性**：CAD模型必须使用米作为单位
5. **遮罩质量**：遮罩质量直接影响位姿估计精度

## 5. 调试选项

运行时可以使用以下调试选项：

- `--debug 1`：基本调试信息
- `--debug 2`：保存中间结果
- `--debug 3`：保存详细调试信息

## 6. 故障排除

1. **内存不足**：降低图像分辨率
2. **位姿估计不准确**：检查遮罩质量和CAD模型精度
3. **文件路径错误**：确认所有文件路径正确
4. **依赖问题**：确保所有依赖库正确安装

通过遵循以上步骤，你应该能够成功使用自定义数据集测试FoundationPose的功能。